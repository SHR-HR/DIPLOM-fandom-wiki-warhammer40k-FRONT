# Базовый URL для FastAPI бэкенда
# Указывает адрес, по которому фронтенд будет обращаться к API
VITE_API_URL=http://localhost:8000

# Режим аутентификации для дипломного проекта:
# local  - имитация входа на клиенте (как требует ТЗ диплома)
#         - аутентификация происходит полностью на фронтенде
#         - не требует работающего бэкенда
# basic  - реальная Basic-авторизация через API (текущая схема бэкенда)
#         - каждый запрос включает заголовок Authorization
VITE_AUTH_MODE=local

# Учетные данные для Basic Auth (используются только при VITE_AUTH_MODE=basic)
# Эти переменные используются для:
# - Автоматической аутентификации в development-режиме
# - Тестирования API эндпоинтов
# - Демонстрации функционала без ручного ввода
VITE_BASIC_USER=admin
VITE_BASIC_PASS=password






# Подробное объяснение файла-шаблона:
# Назначение .env.example
# Этот файл служит шаблоном для создания реального .env файла. Он:

# Показывает все доступные переменные окружения

# Содержит значения по умолчанию для быстрого старта

# Объясняет назначение каждой переменной

# Используется для документации настроек проекта

# VITE_AUTH_MODE - Ключевая настройка для диплома
# local режим (рекомендован для ТЗ)

# // Как работает:
# // 1. Пользователь вводит логин/пароль в форме
# // 2. Валидация происходит на клиенте (без запроса к API)
# // 3. Сессия сохраняется в localStorage
# // 4. Все последующие операции используют моковые данные

# // Преимущества для дипломного проекта:
# // - Не зависит от доступности бэкенда
# // - Предсказуемое поведение при демонстрации
# // - Соответствует требованиям ТЗ


# basic режим (реальная аутентификация)

# // Как работает:
# // 1. Пользователь вводит логин/пароль
# // 2. Отправляется запрос к /login эндпоинту API
# // 3. При успехе возвращается токен/данные пользователя
# // 4. Все последующие запросы включают заголовок Authorization

# // Преимущества:
# // - Реальная интеграция с бэкендом
# // - Проверка паролей на сервере
# // - Актуальные данные из базы

# Учетные данные по умолчанию
# VITE_BASIC_USER=admin
# Логин для автоматической аутентификации

# Может быть изменен на любой существующий в системе

# Используется только в development-режиме

# VITE_BASIC_PASS=password
# Пароль для автоматической аутентификации

# Внимание: Использовать только для тестирования!

# В продакшене никогда не хранить реальные пароли

# Процесс настройки нового окружения:

# # 1. Скопировать шаблон в реальный файл
# cp .env.example .env

# # 2. Настроить под конкретное окружение
# # Для разработки:
# VITE_API_URL=http://localhost:8000
# VITE_AUTH_MODE=local

# # Для тестирования с бэкендом:
# VITE_API_URL=http://localhost:8000
# VITE_AUTH_MODE=basic
# VITE_BASIC_USER=Sasala01
# VITE_BASIC_PASS=N;5Ehazi3Ah#2A(N


# Интеграция с Warhammer 40,000 вики:
# Для демонстрации дипломной работы:

# # Оптимальные настройки для защиты диплома:
# VITE_AUTH_MODE=local
# VITE_API_URL=http://localhost:8000  # на случай если бэкенд доступен

# Для разработки и тестирования:

# # Полный цикл разработки:
# VITE_AUTH_MODE=basic
# VITE_API_URL=http://localhost:8000
# VITE_BASIC_USER=Sasala01
# VITE_BASIC_PASS=N;5Ehazi3Ah#2A(N

# Важные замечания по безопасности:
# .env.example можно коммитить в Git

# Реальный .env нельзя коммитить в Git (добавлен в .gitignore)

# Пароли в .env.example должны быть тестовыми

# Для продакшена использовать .env.production

# Этот шаблон обеспечивает легкий старт проекта и четкое понимание всех доступных настроек для разработчиков,
#  работающих над фронтендом вики по Warhammer 40,000.






