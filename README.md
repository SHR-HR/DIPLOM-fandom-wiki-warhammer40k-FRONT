# Fandom Wiki – Warhammer 40,000

Дипломный фронтенд-проект: одностраничное SPA-приложение (Single Page Application), клиентская часть фанатской вики по вселенной **Warhammer 40,000**.

Проект реализован на **React + TypeScript** с использованием **Redux Toolkit**, **React Router**, **axios**, кастомной темизации (Tailwind + SCSS) и работой с REST API.

> ⚠️ Проект учебный и некоммерческий. Все права на вселенную Warhammer 40,000 принадлежат **Games Workshop**.

---

## Содержание

- [Описание](#описание)
- [Основные возможности](#основные-возможности)
- [Используемые технологии](#используемые-технологии)
- [Требования к окружению](#требования-к-окружению)
  - [Обязательные инструменты](#обязательные-инструменты)
  - [Рекомендуемые дополнительные инструменты](#рекомендуемые-дополнительные-инструменты)
  - [Рекомендуемые расширения-vs-code](#рекомендуемые-расширения-vs-code)
- [Структура проекта](#структура-проекта)
- [Связка с backend-частью](#связка-с-backend-частью)
- [Быстрый старт (локальный запуск)](#быстрый-старт-локальный-запуск)
- [Скрипты npm](#скрипты-npm)
- [Переменные окружения](#переменные-окружения)
- [Архитектура фронтенда](#архитектура-фронтенда)
  - [Состояние](#состояние)
  - [Маршрутизация](#маршрутизация)
  - [Работа с-api](#работа-с-api)
- [Основные пользовательские сценарии](#основные-пользовательские-сценарии)
- [Дальнейшее развитие](#дальнейшее-развитие)
- [Лицензия и права](#лицензия-и-права)

---

## Описание

**Fandom Wiki – Warhammer 40,000** — это одностраничное веб-приложение, в котором:

- Пользователь может просматривать каталог статей по вселенной Warhammer 40k (персонажи, фракции, события).
- Есть детальная страница статьи с превью-изображением, блоком «Главная информация» и основным контентом.
- Реализована авторизация и личный кабинет пользователя с его статьями.
- Поддерживается создание, редактирование и удаление статей (CRUD) через REST API.

Проект выполнен как **дипломная работа** и демонстрирует владение современным фронтенд-стеком.

---

## Основные возможности

- 📚 **Каталог статей**
  - Список статей c карточками (изображение + заголовок).
  - Поиск по заголовкам.
  - Сортировка и пагинация (переключение страниц списка).

- 🧾 **Страница статьи**
  - Крупное превью-изображение (hero).
  - Блок **«Главная информация»** (имя, возраст, рост/вес, внешность и т.п.).
  - Основной контент: заголовки, абзацы, списки, изображения.

- 👤 **Авторизация и профиль**
  - Вход по логину и паролю (Basic Auth, интеграция с API).
  - Сохранение сессии в `localStorage`.
  - Страница профиля с именем, аватаром и мини-статистикой (количество статей, дата регистрации — при наличии на backend’е).
  - Список собственных статей: быстрый переход к редактированию или удалению.

- ✏️ **Работа со статьями (CRUD)**
  - Создание новой статьи.
  - Редактирование существующей.
  - Удаление (с подтверждением).
  - Разделение данных на превью-изображение, основное изображение персонажа и контентные блоки.

- 🎨 **Интерфейс и темизация**
  - Тёмная и светлая тема с кастомным оформлением в стилистике Warhammer 40k.
  - Адаптивная верстка (desktop / tablet / basic mobile).
  - Компонент `SmartImg` для корректной загрузки внешних изображений (прокси, fallback, плейсхолдеры).

---

## Используемые технологии

### Ядро фронтенда

- [React](https://react.dev/) (SPA)
- [TypeScript](https://www.typescriptlang.org/)
- [Vite](https://vitejs.dev/) — сборка и dev-сервер

### Состояние и маршрутизация

- [Redux Toolkit](https://redux-toolkit.js.org/) — глобальное состояние (авторизация, статьи, профиль и т.п.)
- [React Router DOM](https://reactrouter.com/) — маршруты: главная, статья, профиль, создание/редактирование, 404

### Работа с API и валидация

- [axios](https://axios-http.com/) — HTTP-клиент, централизованный конфиг и перехватчики
- [zod](https://zod.dev/) — валидация данных форм (например, профиля)

### UI и стили

- [Tailwind CSS](https://tailwindcss.com/)
- SCSS (`src/styles/index.scss`) — глобальная тема, анимации, рамки, оформление тем
- [lucide-react](https://lucide.dev/) — иконки
- [react-hot-toast](https://react-hot-toast.com/) — уведомления

---

## Требования к окружению

### Обязательные инструменты

- **Node.js** (LTS, рекомендуется 18+)
- **npm** (ставится вместе с Node.js) или **Yarn**
- **Git** — работа с репозиторием
- **Visual Studio Code** — основная IDE, под которую нацелен проект

### Рекомендуемые дополнительные инструменты

Не все из них строго обязательны для запуска фронтенда, но полезны для полного рабочего окружения:

- **Docker Desktop** — для контейнеризации backend-API.
- **Python 3.x** — если вы собираетесь запускать связанный backend (FastAPI).
- **Postman** или **Insomnia** — для тестирования REST-эндпоинтов API.
- **PowerShell** (Windows) или **Git Bash / WSL** — для удобной работы с CLI и скриптами.

### Рекомендуемые расширения VS Code

- `ESLint` — проверка качества кода
- `Prettier` — автоформатирование
- `Tailwind CSS IntelliSense` — подсказки по классам Tailwind
- `Docker` — если используете Docker
- `Python` — если запускаете backend
- `PowerShell` — для запуска PowerShell-скриптов

---

## Структура проекта

## Упрощённая структура фронтенда (по папке `FRONT`):

```text
FRONT/
├─ public/                      # Статические файлы
├─ src/
│  ├─ app/
│  │  ├─ store.ts               # Конфигурация Redux store
│  │  ├─ hooks.ts               # useAppDispatch / useAppSelector
│  │  └─ router.tsx             # Основной роутер приложения
│  │
│  ├─ pages/
│  │  ├─ HomePage.tsx           # Главная: список статей
│  │  ├─ ArticlePage.tsx        # Страница конкретной статьи
│  │  ├─ NewArticlePage.tsx     # Создание статьи
│  │  ├─ EditPage.tsx           # Редактирование статьи
│  │  ├─ ProfilePage.tsx        # Профиль пользователя
│  │  ├─ RegisterPage.tsx       # Регистрация
│  │  └─ errors/
│  │      ├─ ErrorPage.tsx      # Общая ошибка приложения
│  │      ├─ NetworkErrorPage.tsx # Ошибка сети / API
│  │      └─ NotFoundPage.tsx   # 404
│  │
│  ├─ features/
│  │  ├─ articles/
│  │  │  ├─ ArticleCard.tsx     # Карточка статьи (превью)
│  │  │  ├─ ArticleList.tsx     # Список статей
│  │  │  ├─ ArticleView.tsx     # Детальное отображение статьи
│  │  │  ├─ AddImageBlock.tsx   # Блок с изображением в редакторе
│  │  │  └─ articlesSlice.ts    # Redux-слайс статей
│  │  │
│  │  ├─ auth/
│  │  │  ├─ authSlice.ts        # Состояние авторизации
│  │  │  ├─ LoginForm.tsx       # Форма входа
│  │  │  └─ AuthGate.tsx        # UI-гейт/защита маршрутов
│  │  │
│  │  ├─ profile/
│  │  │  └─ profileSlice.ts     # Профиль и «мои статьи»
│  │  │
│  │  ├─ editor/
│  │  │  └─ editorSlice.ts      # Состояние редактора контента
│  │  │
│  │  └─ ui/
│  │     ├─ ImageInput.tsx
│  │     ├─ Paginator.tsx
│  │     ├─ SearchInput.tsx
│  │     ├─ SortControl.tsx
│  │     ├─ SortSelect.tsx
│  │     ├─ ThemeToggle.tsx
│  │     └─ uiSlice.ts          # Вспомогательные UI-флаги
│  │
│  ├─ components/
│  │  ├─ SmartImg.tsx           # Обёртка для изображений
│  │  ├─ ContentBlockEditor.tsx # Редактор блоков контента
│  │  ├─ ContentPreview.tsx     # Превью контента статьи
│  │  ├─ routing/
│  │  │  └─ RequireAuth.tsx     # HOC для защищённых маршрутов
│  │  └─ forms/
│  │     └─ EditorImagePreview.tsx
│  │
│  ├─ api/
│  │  ├─ client.ts              # axios-клиент (baseURL, interceptors)
│  │  └─ endpoints.ts           # Обёртки над REST-эндпоинтами
│  │
│  ├─ models/
│  │  └─ api.ts                 # Типы ответов API (статьи, профиль и т.п.)
│  │
│  ├─ utils/
│  │  ├─ fileUpload.ts          # Вспом. логика загрузки файлов (если используется)
│  │  ├─ imgProxy.ts            # Проксирование изображений, placeholder
│  │  ├─ sortArticles.ts        # Сортировка списка статей
│  │  └─ validators.ts          # Схемы валидации (zod)
│  │
│  ├─ seed/
│  │  ├─ articles.ts            # Укороченные сид-данные
│  │  └─ fullArticles.ts        # Полные сид-данные для отладки
│  │
│  └─ styles/
│     └─ index.scss             # Глобальные SCSS + интеграция с Tailwind
│
├─ .env.example                 # Шаблон переменных окружения
├─ package.json
├─ tsconfig.json / tsconfig.app.json
├─ vite.config.ts
└─ README.md



Связка с backend-частью

Фронтенд рассчитан на работу с отдельным REST-API (FastAPI):

репозиторий backend-части: папка API в этом же проекте (отдельный README см. там);

основная точка входа API — http://localhost:8000.

Адрес API передаётся через переменную окружения VITE_API_URL (см. ниже).



Быстрый старт (локальный запуск)

Ниже — универсальный вариант. Если репозиторий общий, сначала нужно попасть в папку FRONT.

1. Клонировать репозиторий

---

git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>/FRONT   # либо сразу в папку FRONT, если репозиторий только с фронтом


Замените <your-username> и <your-repo> на свой логин GitHub и название репозитория.

2. Установить зависимости

```

npm install
# или
yarn install

```


3. Настроить переменные окружения

Если есть файл .env.example, скопируйте его:

```

# Linux / macOS
cp .env.example .env

# Windows (PowerShell / CMD)
copy .env.example .env

```


Если .env.example нет — создайте .env вручную (см. раздел «Переменные окружения»).

Минимально нужна переменная:

VITE_API_URL=http://localhost:8000


4. Запустить dev-сервер

```
npm run dev
# или
yarn dev

```

По умолчанию Vite поднимет фронтенд по адресу:

http://localhost:5173

Для полноценной работы (CRUD, авторизация) должен быть запущен backend-API из папки API.

```

{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}

```

Чаще всего используются:

npm run dev — запуск проекта в режиме разработки;
npm run build — production-сборка в папку dist/;
npm run preview — просмотр уже собранной версии локально.

---


Переменные окружения

Фронтенд ожидает адрес backend-API через переменную среды, например:

VITE_API_URL=http://localhost:8000


Где:

VITE_API_URL — базовый URL до REST API (локальный или удалённый).

Использование в src/api/client.ts (упрощённый пример):


const baseURL = import.meta.env.VITE_API_URL ?? "http://localhost:8000";

export const client = axios.create({
  baseURL,
  timeout: 10000,
});

Если API запущен на другом порту или домене — укажите его в .env.

---


Архитектура фронтенда
Состояние

Используется Redux Toolkit:

articlesSlice — список статей, отдельная статья, состояния загрузки/ошибок.

authSlice — авторизация (isAuthed, данные пользователя, режим local/api и т.п.).

profileSlice — данные профиля и список «моих статей».

editorSlice — состояние редактора контента (блоки статьи).

uiSlice — вспомогательные UI-флаги (фильтры, тема, загрузка и т.д.).

Кастомные хуки:

useAppDispatch / useAppSelector — типобезопасная работа с Redux в компонентах.

Маршрутизация

Используется react-router-dom:

/ — главная страница (каталог статей).

/article/:id — детальная страница статьи.

/profile — профиль авторизованного пользователя.

/new — создание статьи.

/edit/:id — редактирование статьи.

/register — регистрация.

/error и /network-error — страницы ошибок.

* — страница 404.

Защищённые маршруты оборачиваются в компоненты RequireAuth / AuthGate, которые перенаправляют неавторизованных пользователей на страницу входа и могут запоминать путь возврата.

Работа с API

Все HTTP-запросы проходят через единый axios-клиент:

baseURL собирается из VITE_API_URL;

перехватчики добавляют заголовки авторизации (Basic Auth);

при ответах 401/403 маршрут, с которого «выкинуло», запоминается через RETURN_TO_KEY в sessionStorage и используется для возврата после повторного входа.

Основные сущности API:

Статьи — /articles, /articles/{id} (список, детальная, создание, редактирование, удаление);

Профиль — /myProfile, /myArticles, /updateUser;

Авторизация — /login, /register.

Основные пользовательские сценарии

Гость просматривает статьи

Заходит на /.

Видит список статей в виде карточек с изображением и названием.

Может пользоваться поиском и сортировкой (по новизне, по алфавиту, по автору и т.п.).

Переходит на интересующую статью.

Просмотр конкретной статьи

Переход по карточке или прямой ссылке /article/:id.

Вверху — крупное превью-изображение.

В блоке «Главная информация» — ключевые характеристики персонажа/фракции.

Ниже — основной контент: текст, списки, изображения, оформленные блоками.

Авторизация и профиль

Переход к форме входа / регистрации.

Ввод логина и пароля → запрос к API → сохранение сессии.

В профиле (/profile) отображаются имя, аватар, статистика и список собственных статей.

Можно изменить имя и URL аватарки (валидация через zod).

Создание / редактирование статей

На /new пользователь заполняет форму: заголовок, превью-изображение, основная информация, блоки контента.

Кнопка «Создать статью» активна только при корректно заполненных полях.

После сохранения статья появляется в общем каталоге и в разделе «Мои статьи».

На /edit/:id можно обновить статью или удалить её.

Дальнейшее развитие

Возможные направления развития проекта:

Расширенный поиск и фильтры (по фракции, эпохе, типу персонажа и т.д.).

Поддержка нескольких фэндомов (не только Warhammer 40,000).

Более гибкая система ролей (администратор / редактор / читатель).

Загрузка локальных файлов и менеджер изображений на сервере.

Комментарии, рейтинги статей, избранное.

Расширенные настройки профиля и тем (несколько цветовых схем, пользовательские пресеты).

Интернационализация (RU/EN и т.д.).

Лицензия и права

Проект создан в учебных целях как дипломная работа.

Весь контент вселенной Warhammer 40,000 является собственностью Games Workshop.
Использование названий, изображений и описаний лора носит некоммерческий демонстрационный характер.

---


